import styled from 'styled-components';

const RobotoButton = styled.button`
    font-family: roboto;
    font-size: 40px;
`;

const OpenSansButton = styled.button`
    font-family: open-sans;
    font-size: 40px;
`;

export {
    RobotoButton,
    OpenSansButton
}